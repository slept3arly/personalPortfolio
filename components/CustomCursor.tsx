"use client";

// Custom cursor dropped by user request.
// System cursor is active. This component is kept as a shell
// so the layout.tsx import doesn't break.
export default function CustomCursor() {
  return null;
}
